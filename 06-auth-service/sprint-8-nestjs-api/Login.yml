info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: auth/login
  body:
    type: json
    data: |-
      {
        "username": "alice",
        "password": "mission123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
