info:
  name: Check Credentials
  type: http
  seq: 3

http:
  method: POST
  url: verify
  body:
    type: json
    data: |-
      {
        "username": "alice"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
