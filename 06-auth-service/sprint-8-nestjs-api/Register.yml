info:
  name: Register
  type: http
  seq: 4

http:
  method: POST
  url: auth/register
  body:
    type: json
    data: |-
      {
        "username": "alice",
        "password": "mission123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
