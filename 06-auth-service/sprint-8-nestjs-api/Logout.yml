info:
  name: Logout
  type: http
  seq: 7

http:
  method: POST
  url: auth/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
