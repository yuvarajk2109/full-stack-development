package com.neueda.leap.sprint5;

// Kata A: this is the BAD version - a public, unvalidated field. Do not fix this
// file directly. Instead, write Holding.java (a separate class in this same
// package) that fixes the underlying problem properly. See README.md for what
// Holding.java must do. This file is left here only so you can see what the
// "before" looks like; it is not part of the acceptance criteria.
public class HoldingStarter {
    public double quantity;
}
