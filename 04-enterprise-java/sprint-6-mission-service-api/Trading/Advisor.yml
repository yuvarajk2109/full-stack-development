info:
  name: Advisor
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/advisors/1
  auth:
    type: bearer
    token: "{{JWT_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
