info:
  name: Client Portfolio
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/portfolio/P1
  auth:
    type: bearer
    token: "{{JWT_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
