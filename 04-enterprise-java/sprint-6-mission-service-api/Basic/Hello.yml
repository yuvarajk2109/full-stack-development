info:
  name: Hello
  type: http
  seq: 1

http:
  method: GET
  url: hello
  auth:
    type: bearer
    token: "{{JWT_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
