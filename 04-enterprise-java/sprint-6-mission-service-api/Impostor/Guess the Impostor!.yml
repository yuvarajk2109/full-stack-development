info:
  name: Guess the Impostor!
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/impostor/1
  auth:
    type: bearer
    token: "{{JWT_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
