info:
  name: Order Fee
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/orders/AAPL/fee?tradeValue=10000
  params:
    - name: tradeValue
      value: "10000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
