info:
  name: Get JWT Token
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4000/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
