info:
  name: Get JWT Token
  type: http
  seq: 3

http:
  method: POST
  url: localhost:4000/login
  body:
    type: json
    data: |-
      {
        "username": "alice",
        "password": "mission123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
